name: CI
on:
  push: {}
jobs:
  check_rotating_lights:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - run: git grep -n 🚨 | sed -E 's/^([^:]+):([^:]+):\s*/::error file=\1,line=\2::/'
